import { Routes } from '@angular/router';
import {CorrelationComponent} from "./correlation/correlation.component";

export const routes: Routes = [
  { path: 'plot-correlation', component: CorrelationComponent },
];
